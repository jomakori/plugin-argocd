[package]
name = "plugin-argocd"
version = "0.1.0"
edition = "2021"
description = "OpenKite ArgoCD JS plugin — manifest validator + JS lint harness."
license = "MIT OR Apache-2.0"
publish = false

[[bin]]
name = "validate-manifest"
path = "bin/validate-manifest.rs"

[dependencies]
serde_json = "1"
